apiVersion: v1
kind: Service
metadata:
  name: kuliaev-diplom-service
  namespace: default
spec:
  type: NodePort
  selector:
    app: web-app
  ports:
  - port: 80           # порт внутри кластера
    targetPort: 80     # порт в контейнере
    nodePort: 30081  
